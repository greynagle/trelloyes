import React from 'react';
import STORE from './store.js';


function App() {
  const columns=STORE.lists;
  const fill=STORE.allCards;
  let result="result"
  console.log(columns)
  console.log(fill)

  return (
    <main className='App'>
      {for (let index = 0; index < array.length; index++) {
        const element = array[index];
        
      }}
    </main>
  );
}

export default App;